# Cloudflare Pages redirects
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# www → non-www is handled by setting the apex domain as "Primary"
# in Cloudflare Pages > Custom domains. No rule needed here.
#
# Add relative-path redirects below if needed, e.g.:
# /old-page /new-page 301
